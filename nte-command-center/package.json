{
  "name": "nte-command-center",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run check:lanes && npm run check:network && tsc -b && vite build",
    "preview": "vite preview",
    "check:types": "tsc -b --noEmit",
    "check:lanes": "node scripts/check-lanes.mjs",
    "check:network": "node scripts/check-network.mjs",
    "test": "vitest run",
    "check": "npm run check:types && npm run check:lanes && npm run check:network && npm test",
    "predev": "npm run check:network",
    "pretest": "npm run check:network"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.9"
  }
}
